import 'package:flutter/material.dart';

class AppColors {
  // Primary Color
  static const Color primaryColor = Color(0xFF457887);

  // Secondary Color
  static const Color secondaryColor = Color(0xFF123456);

  // Accent Color
  static const Color accentColor = Color(0xFFABCDEF);

  // Background Colors
  static const Color backgroundColor = Color(0xFFE8E8E8);
  static const Color scaffoldBackgroundColor = Color(0xFFF5F5F5);

  // Text Colors
  static const Color textPrimaryColor = Color(0xFF000000);
  static const Color textSecondaryColor = Color(0xFF757575);
  static const Color btnTextColor = Colors.white;

  // Button colors
  static const Color buttonColor = Color(0xFF457887);

  // Icon Colors
  static const Color iconColor = Color(0xFF757575);

  // Border Color
  static const Color borderColor = Color(0xFF457887);

  // Divider Color
  static const Color dividerColor = Color(0xFFBDBDBD);

  // Error Color
  static const Color errorColor = Color(0xFFFF0000);
}
